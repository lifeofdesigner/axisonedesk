# Copy this file to .env.local (gitignored — never commit the real values)
# and fill in the real service_role key from your Supabase project settings
# (Project Settings -> API -> service_role secret key).
#
# This key bypasses ALL row-level security. It must never be pasted into
# the main app's .env, never referenced from src/, and this tool must never
# be deployed publicly — it is local-only by design.
SUPABASE_URL=https://yscvwtcrtbcfpkwtinvv.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
